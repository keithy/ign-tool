# Configuration file
# (add to .gitignore)

#THEME=0 # simplifies testing

# Directly to a USB KEY
# deploy_target=/Volumes/Key/ignition.json
# Local file
deploy_target=./ignition.json

# File for local output .json/.yaml
output=tmp.json
trash=~/.Trash

header="$workingDir/input/00-header.yaml"

forms="$commandDir/forms_ignition"
workspace="$workingDir/input"

libraries=(
    "$workingDir/plugs"
    "$commandDir/plugs"
)
