# Configuration file
# (add to .gitignore)

THEME=0 # simplifies testing

# Directly to a USB KEY
deploy_target=./ignition.json

# File for local output .json/.yaml
output=tmp.json
trash=~/.Trash

header="00-header.yaml"

workspace="$workingDir/input"

libraries=(
    "$workingDir/plugs"
    "$commandDir/plugs"
)
